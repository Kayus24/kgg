    window.compactKggH2Exercise=function(ex){const row=compact.apply(this,arguments),values=normalizeVariants(ex,false);if(!values.length)return row;let wire=row[11]&&typeof row[11]==='object'&&!Array.isArray(row[11])?clone(row[11]):null;if(!wire&&window.KGGTicket015Admin&&typeof window.KGGTicket015Admin.wireVariants==='function')wire=clone(window.KGGTicket015Admin.wireVariants(ex));if(wire){wire.g=groupId(ex);wire.a=mainIdFor(ex,values);row[11]=wire;}return row;};
    window.expandKggH2Exercise=function(item){const ex=expand.apply(this,arguments),wire=Array.isArray(item)&&item[11]&&typeof item[11]==='object'&&!Array.isArray(item[11])?item[11]:null;if(wire&&wire.a){ex.progressionMainId=String(wire.a);if(!ex.progressionVariants&&window.KGGTicket015Admin&&typeof window.KGGTicket015Admin.variantsFromWire==='function'){const values=window.KGGTicket015Admin.variantsFromWire(wire,ex);if(values.length)ex.progressionVariants=values;}}return ex;};
    window.buildPatientExercisePayload=function(ex){const copy=patient.apply(this,arguments),values=normalizeVariants(ex,false);if(values.length){copy.progressionGroupId=groupId(ex);copy.progressionMainId=mainIdFor(ex,values);}return copy;};
  }
  function init(){ensureStyle();patchSerializers();patchEditor();setTimeout(patchSerializers,200);setTimeout(patchEditor,200);setTimeout(patchSerializers,800);setTimeout(patchEditor,800);}
  window.KGGTicket015AdminGallery={version:VERSION,normalizeVariants,mainIdFor,moveVariant:(items,from,to)=>{const next=items.slice();const item=next.splice(from,1)[0];next.splice(to,0,item);return next;}};
  if(window.__KGG_TEST__)window.__kggTicket015AdminGalleryTest={normalizeVariants,mainIdFor};
  document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init,{once:true}):init();
})();
</script>
<!-- KGG PATCH END kgg-v090-ticket-015-admin-stage-gallery-drag-motion -->
