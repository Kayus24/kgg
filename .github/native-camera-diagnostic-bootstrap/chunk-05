lib.decompress(base64.b64decode(ACTIVITY_ZLIB_B64)).decode("utf-8"),
        encoding="utf-8",
        newline="\n",
    )
    bootstrap.write_text(
        zlib.decompress(base64.b64decode(BOOTSTRAP_ZLIB_B64)).decode("utf-8"),
        encoding="utf-8",
        newline="\n",
    )
    print("Applied native camera diagnostic source transformation")
    for path in (gradle, manifest, main_activity, bootstrap, activity):
        print(path.relative_to(ROOT))


if __name__ == "__main__":
    main()
